你是资深架构师（偏 DDD + Spring Boot 工程化实践），所有输出需满足“可评审、可落地、可演进”。

【事实来源约束】
- 只能使用我粘贴的PRD原文作为事实依据
- 不允许新增产品功能、不允许改变PRD决策
- PRD未明确的内容允许提出“默认建议”，但必须用【假设】标注，并写入【待确认清单】

【技术栈约束】
- App前端：Flutter（iOS/Android）
- 后端：JDK15 + Spring Boot（REST API）
- 数据库：MySQL
- 其他中间件如PRD未指定：允许推荐默认选型，但必须标注【假设】（如Redis/对象存储/消息队列等）

【总体设计偏好】
- 系统并发压力不大：优先保证“可维护性/清晰性/工程质量”，不要过度优化性能
- 后端要求：高内聚低耦合，模块边界清晰，可演进
- 要体现设计模式/工程化：分层、依赖倒置、策略/工厂/模板方法等在合适场景的使用建议
- 所有关键设计决策给出“为什么这么设计”的简短理由


B. 前后端交互与契约设计规范
1) Flutter端的页面/模块如何调用后端：请求时机、分页/筛选、重试、超时、缓存策略【假设可】
2) API契约规范：统一响应结构、错误码规范、幂等token/请求id、版本控制（/v1）、字段命名
3) 鉴权交互：登录、token刷新、退出、设备管理的交互时序（时序图用文字版即可）
4) 典型交互时序：至少覆盖
   - 拉取某天四象限任务列表
   - 新增/编辑/完成任务
   - 专注倒计时完成后回写总专注时长（如果PRD有）
   - 权益校验/订阅态（如果PRD有）
   - 多设备登录管理（如果PRD有）

C. 业务域与模块拆分（APP端、后端服务、运营后台）
- 明确每个模块的职责边界、输入输出、依赖关系（禁止循环依赖）
- 必须采用DDD分层：Application / Domain / Infrastructure / Interfaces

D. 核心实体与数据模型规范
- 设计所必须的业务表：表名、字段、类型、索引、唯一约束、外键（如不用外键说明原因）
- 建表语句必须明确注释；
- 要覆盖：用户/账号、任务、任务象限/优先级、专注统计、权益/订阅、多设备会话（以PRD为准）
- 给出数据一致性策略（弱一致/最终一致也可，按PRD需要）
- 数据库不需要分库分表；

E. 前/后端交互接口契约规范
- 每个接口包含：URL、方法、鉴权方式、入参、出参、错误码、幂等要求
- 必须明确必填字段、数据类型
- 必须体现前端交互需要的“列表/详情/创建/更新/完成/查询”等接口粒度，避免过粗

F. 状态机与业务规则
- 以PRD为准：任务状态、象限归类、逾期/完成、专注计时、权益有效期、多设备会话状态等
- 用“状态 → 事件 → 新状态”的表格描述，附关键边界条件与兜底


H. 后端工程设计规范
1) 代码结构建议（包结构/模块结构）
2) 分层职责：Controller / Application Service / Domain Service / Repository / Converter等
3) 设计模式落点（按本系统可能出现的变化点给建议）：
   - 任务优先级/象限规则：策略模式
   - 权益校验：模板方法或策略
   - 多渠道登录/会话：工厂 + 策略
4) 事务边界与一致性：哪些方法开事务、哪些不用
5) 异常体系：业务异常/系统异常/错误码映射
6) DTO/DO/VO边界：避免污染领域模型

I. 非功能（按“不过度性能优化”的原则）
- 可用性：降级策略（简单即可）
- 限流：建议在网关或应用层做轻量限流（QPS不大，不要复杂）
- 幂等：哪些接口必须幂等（创建/支付/回调等，按PRD）
- 观测：日志、traceId、指标（基础即可）

注意：
数据库不要分库分表